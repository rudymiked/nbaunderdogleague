import React from "react";
import { Container, Row, Col } from "react-bootstrap";

interface IInvitationsProps {
}

export const Invitations: React.FunctionComponent<IInvitationsProps> = (props: IInvitationsProps) => {
    return (
        <>
            <Container>
                <Row>
                    <Col>
                    </Col>
                </Row>
            </Container>
        </>
    );
}